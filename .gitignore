# Entornos virtuales
.venv/
venv/
env/
ENV/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Modelos YOLO (pesos grandes — el usuario los tiene local)
*.pt
*.onnx
*.engine
*.tflite

# Datasets y runs
runs/
runs_*/
data_microplastico/
Imagenes Microplasticos.rar
Fotos de peces y camarones/
# Fotos de trabajo para resolver que borde de la placa son los 100 mm
Fotos Placa medida/
imagenes max/
Microscopio/
data/
datasets/

# Avance / docs personales
Avance MP/
Papers Analítica/
Proyecto de Titulo/
YOLO/
YOLO Python/
*.docx
*.pptm
*.pptx
~$*
PROCEDIMIENTO*.docx

# Reportes generados
reporte_paper*.html
reporte_*.html

# IDE
.vscode/
.idea/
*.swp

# OS
Thumbs.db
.DS_Store
desktop.ini
# Copias en conflicto que crea OneDrive al sincronizar desde dos equipos
*-NeWsOmen.*
*[Cc]opia en conflicto*

# Logs y temporales
*.log
*.tmp
venv.txt

# Estado local del actualizador (SHA del commit instalado)
.polyx_version

# Datos de active learning (imagenes pesadas, no van al repo)
dataset_correcciones/
# Scripts temporales de diagnostico (agrupados en _diagnostico/ desde sep-2026)
_diagnostico/
_diag_*.py
_diag_overlays/
_diag_train_vis/
_tmp_*.yaml
PolyX_dataset.zip

# ── Material de investigacion sin publicar ──────────────────────────
# ESTE REPOSITORIO ES PUBLICO y documenta SOLO el programa. Todo lo que forme
# parte de un paper en preparacion se queda fuera: publicarlo aqui adelantaria
# resultados propios y ajenos.
fotos sed testigos/
PolyX_recortes/
# paper/ = pipeline de analisis, borrador de Metodos y hallazgos del estudio
paper/

# Pesos entrenados (500 MB) y carpetas de trabajo pesadas
Wheigts/
poly-x-repo/

# MATLAB: instalador de 220 MB (GitHub rechaza >100 MB) y producto licenciado.
# El instalador y su carpeta temporal viven dentro de 'Microplasticos con MATLAB/'.
Microplasticos con MATLAB/
matlab_R2024b_Windows.exe

# ── Fuera del repo publicado: no lo necesita el programa ────────────
# El repositorio contiene SOLO lo que hace falta para instalar, ejecutar y
# documentar Poly-X. Lo de abajo se queda en el disco de trabajo.
# Los PDF del manual se regeneran con: generar_manual.py --pdf
# El HTML si va versionado y se abre sin conexion, asi que el PDF sobra (10 MB).
Manual_PolyX*.pdf

# Hoja de ruta personal, no es material del programa.
SUGERENCIAS_Y_PROXIMOS_PASOS.md
# Guias y manuales de terceros (Claude Code, etc.): no son de este proyecto.
Guia_*.html
Guia_*.pdf

# Herramientas locales de agentes
.agents/
.claude/
_cand.pkl
